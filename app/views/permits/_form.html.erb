<%= form_for(@permit) do |f| %>
  <% if @permit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permit.errors.count, "error") %> prohibited this permit from being saved:</h2>

      <ul>
      <% @permit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  </div>
  <div class="field">
    <%= f.label :permitStartDate, "Permit Start Date" %><br>
    <%= f.date_select :permitStartDate %>
  </div>
  <div class="field">
    <%= f.label :permitExpDate, "Permit Expiration Date" %><br>
    <%= f.date_select :permitExpDate %>
  </div>
  <div class="field">
    <%= f.label :propertyID, "Property Name" %><br>
    <%= f.collection_select :fkPropertyKey, Property.all, :id, :propertyName%>
  </div>
    <div class="field">
      <%= f.label :statusID, "Status" %><br>
      <%= f.collection_select :fkStatusKey, Status.all, :id, :description%>
    </div>


  <div class="field">
    <%= f.label :siteType, "Site Type" %><br>
    <%= f.select :siteType, ['Site A','Site B','Site C']%>
  </div>
  <div class="field">
    <%= f.label :note, "Notes" %><br>
    <%= f.text_field :note %>
  </div>
  <div class="field">
    <%= f.label :amountDue, "Amount Due" %><br>
    <%= f.number_field :amountDue %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
